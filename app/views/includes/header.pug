header
  if path != "/login"
    if  currentUser
      div.profile-info
        h3 Welcome: #{currentUser.username}
        div.logout
          a(href='/logout') Logout
  div.title-heading
    h1.title-text: a(href="/") Link Archive
    
